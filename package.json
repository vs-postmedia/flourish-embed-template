{
  "name": "flourish-embed-template",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.html dist/ && cp -r css dist/ && cp -r fonts dist/ && cp -r images dist/ && cp icon.png dist/",
    "deploy": "npm run build && gh-pages -d dist --nojekyll"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
